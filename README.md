# Visits Counter (Go + Kubernetes + Redis)

Мини-проект написанный на Go во время изучения Kubernetes.
Приложение считает количество посещений, сохраняя состояние в Redis,
и развёртывается в Kubernetes при помощи Deployment, Service, Ingress и TLS.

---

## 🚀 Возможности

* Go-сервис, который возвращает JSON с количеством посещений
* Хранение состояния в Redis
* Kubernetes Deployment + Service (ClusterIP)
* Ingress с собственным доменным именем `visits.local`
* HTTPS через самоподписанный TLS-сертификат
* Liveness / Readiness пробы
* Конфигурация через ConfigMap и Secret
* Локальный запуск в Minikube

---

## 🧱 Структура проекта

```
/cmd              # исходники Go-сервиса
/Dockerfile       # сборка контейнера
/k8s              # Kubernetes манифесты (Deployment, Service, Ingress, Redis)
/tls              # локальные TLS-файлы (ключи и сертификаты не хранятся в Git)
```

---

## 🐳 Запуск локально

```bash
    docker build -t visits-app:local .
    kubectl apply -f k8s/
```

---

## 🔐 TLS

TLS-ключи не хранятся в репозитории. Файлы `tls/*.key` и `tls/*.crt` добавлены в `.gitignore`.

---

## 📌 Цель проекта

Проект создан для практики Kubernetes и демонстрации ключевых возможностей:

* контейнеризация приложения,
* хранение состояния во внешнем сервисе,
* конфигурация окружения,
* маршрутизация трафика через Ingress,
* работа с TLS,
* пробами готовности и живучести контейнера.
